# AWS SDK
# NOTE: boto3 is bundled into the Lambda package (see the deploy createPythonFunction
# helper), so this floor controls the deployed SDK version, not the runtime-provided
# one. 1.43.0 is the minimum that includes the Elemental Inference SubtitlingConfig
# output model required for Smart Subtitles.
boto3>=1.43.0

# AWS Lambda Powertools and dependencies
aws-lambda-powertools>=2.30.0
aws-xray-sdk>=2.12.0
fastjsonschema>=2.16.0
